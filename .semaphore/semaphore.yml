version: 'v1.0'
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: "Run ruby tests"
    task:
      jobs:
        - name: Ruby
          commands:
            - which node
            - node -v
            - nvm ls
            - nvm install 10.15.3
            - nvm uninstall 8.11.3
            - nvm ls
            - export PATH=/home/semaphore/.nvm/versions/node/v10.15.3/bin/:$PATH
            - sem-version ruby 2.5.3
            - checkout
            - cd ruby
            - bundle install
            - which node
            - node -v
            - bundle exec rake
