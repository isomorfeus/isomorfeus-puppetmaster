version: 2.1
jobs:
  build:
    machine: ubuntu-1604:201903-01
    steps:
      - run:
          name: Run ruby test
          command: |
            cd ruby
            bundle install
            bundle exec rake